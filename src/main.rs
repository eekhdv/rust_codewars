use divisors::run;

mod divisors;
mod is_square;
mod open_or_senior;
mod find_next_square;
mod find_outlier;

fn main() {
    /* call functions here: */
    /* 
    * divisors::run(integer);
    * is_square::run(n);
    * open_or_senior;:run(integer);
    * find_next_square::run(sq);
    * find_outlier::run(values);
    */
}


